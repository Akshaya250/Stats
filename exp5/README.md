# exp5

Description
- This folder contains an experiment script `exp5.py`. This experiment likely uses a dataset or different inputs; no `diabetes.csv` is present in this folder.

Files
- `exp5.py`: Experiment script (check the file to see required inputs and behavior).

Requirements
- Python 3.8+
- pandas, scikit-learn, numpy (if used by `exp5.py`)

Run
```bash
python exp5.py
```

Notes
- Open `exp5.py` to confirm any dataset paths or command-line arguments it requires.
